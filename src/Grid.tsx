import React from "react";

const Grid = (props: {
  matrix: (number | string)[][];
  matrixLength: number;
  matrixWidth: number;
  isRunning: boolean;
  handleGridItemClick: (index: number) => void;
}) => {
  return <div></div>;
};

export default Grid;
